import Header from "@/components/core/Header";
import React from "react";

type Props = {};

const Page = (props: Props) => {
  return (
    <main className="">
      <Header />
    </main>
  );
};

export default Page;
